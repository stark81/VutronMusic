# # name: Release

# # on:
# #   push:
# #     tags:
# #       - v*
# #   workflow_dispatch:

# # permissions: write-all

# # jobs:
# #   build-mac-x64:
# #     runs-on: macos-latest
# #     name: Build macOS x64
# #     steps:
# #       - uses: actions/checkout@v4
# #         with:
# #           submodules: recursive

# #       - uses: actions/setup-node@v4
# #         with:
# #           node-version: 22
# #           cache: yarn

# #       - name: Install build essentials (macOS)
# #         run: brew install rpm

# #       - name: Install deps
# #         run: yarn

# #       - name: Build x64
# #         run: |
# #           npm rebuild --arch=x64
# #           ./node_modules/.bin/electron-rebuild --arch=x64

# #       - name: Package x64
# #         shell: bash
# #         env:
# #           GH_TOKEN: ${{ secrets.github_token }}
# #           ARCH: x64
# #         run: yarn build --arch=x64 || yarn build --arch=x64

# #       - name: Upload mac-x64 artifacts
# #         uses: actions/upload-artifact@v4
# #         with:
# #           name: mac-x64
# #           path: release/*/VutronMusic*_x64.dmg

# #   build-mac-arm64:
# #     runs-on: macos-latest
# #     name: Build macOS arm64
# #     steps:
# #       - uses: actions/checkout@v4
# #         with:
# #           submodules: recursive

# #       - uses: actions/setup-node@v4
# #         with:
# #           node-version: 22
# #           cache: yarn

# #       - run: |
# #           brew install rpm
# #           yarn

# #       - name: Build arm64
# #         run: |
# #           npm rebuild --arch=arm64
# #           npx electron-rebuild --arch=arm64
# #           yarn build:pre
# #           yarn electron-builder --mac --arm64 -p never

# #       - name: Upload mac-arm64 artifacts
# #         uses: actions/upload-artifact@v4
# #         with:
# #           name: mac-arm64
# #           path: release/**/*mac_arm64.*

# #   build-linux:
# #     runs-on: ubuntu-latest
# #     name: Build Linux x64
# #     env:
# #       XVFB_PFX: ${{ 'xvfb-run --auto-servernum -- ' }}

# #     steps:
# #       - uses: actions/checkout@v4
# #         with:
# #           submodules: recursive

# #       - uses: actions/setup-node@v4
# #         with:
# #           node-version: 22
# #           cache: yarn

# #       - run: ${{ env.XVFB_PFX }}yarn

# #       - name: Build Linux
# #         run: |
# #           ${{ env.XVFB_PFX }}npm rebuild
# #           ${{ env.XVFB_PFX }}./node_modules/.bin/electron-rebuild
# #           yarn build --linux -p never || yarn build --linux -p never

# #       - name: Upload Linux artifacts
# #         uses: actions/upload-artifact@v4
# #         with:
# #           name: linux-x64
# #           path: release/**/*linux_x64.*

# #   build-linux-arm64:
# #     runs-on: ubuntu-latest
# #     name: Build Linux arm64
# #     env:
# #       XVFB_PFX: ${{ 'xvfb-run --auto-servernum -- ' }}

# #     steps:
# #       - uses: actions/checkout@v4
# #         with:
# #           submodules: recursive

# #       - uses: actions/setup-node@v4
# #         with:
# #           node-version: 22
# #           cache: yarn

# #       - name: Install dependencies
# #         run: |
# #           sudo apt-get update
# #           sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
# #           ${{ env.XVFB_PFX }}yarn

# #       - name: Build for Linux arm64
# #         run: |
# #           export CFLAGS="--target=aarch64-linux-gnu"
# #           export CXXFLAGS="--target=aarch64-linux-gnu"
# #           export npm_config_arch=arm64
# #           export npm_config_target_arch=arm64
# #           ${{ env.XVFB_PFX }}npm rebuild --arch=arm64
# #           ${{ env.XVFB_PFX }}./node_modules/.bin/electron-rebuild --arch=arm64
# #           yarn build --linux --arm64 -p never || yarn build --linux --arm64 -p never

# #       - name: Upload Linux arm64 artifact
# #         uses: actions/upload-artifact@v4
# #         with:
# #           name: linux-arm64
# #           path: release/**/*linux_arm64.*
# #           if-no-files-found: ignore

# #   build-win:
# #     runs-on: windows-latest
# #     name: Build Windows x64
# #     steps:
# #       - uses: actions/checkout@v4
# #         with:
# #           submodules: recursive

# #       - uses: actions/setup-node@v4
# #         with:
# #           node-version: 22
# #           cache: yarn

# #       - run: yarn

# #       - name: Build Windows
# #         run: |
# #           npm rebuild
# #           npx electron-rebuild
# #           yarn build:pre
# #           yarn electron-builder --win -p never

# #       - name: Upload Windows artifacts
# #         uses: actions/upload-artifact@v4
# #         with:
# #           name: win-x64
# #           path: release/**/*win_x64.*

# #   upload-release:
# #     runs-on: ubuntu-latest
# #     needs:
# #       - build-mac-x64
# #       - build-mac-arm64
# #       - build-linux
# #       - build-linux-arm64
# #       - build-win
# #     name: Upload to GitHub Release
# #     steps:
# #       - uses: actions/checkout@v4

# #       - name: Install GitHub CLI
# #         run: sudo apt install gh -y

# #       - name: Download all artifacts
# #         uses: actions/download-artifact@v4
# #         with:
# #           path: all-artifacts

# #       - name: List all artifacts (for debug)
# #         run: ls -R all-artifacts

# #       - name: Upload all files to GitHub Release
# #         env:
# #           GH_TOKEN: ${{ secrets.github_token }}
# #         run: |
# #           TAG_NAME=${GITHUB_REF##*/}
# #           gh release upload "$TAG_NAME" all-artifacts/**/* --clobber

# name: Release

# on:
#   push:
#     tags:
#       - v*
#   workflow_dispatch:

# permissions: write-all

# jobs:
#   Release:
#     strategy:
#       fail-fast: false
#       matrix:
#         os: [windows-latest, macos-latest, ubuntu-latest]
#         arch: [x64]
#         include:
#           - os: macos-latest
#             arch: arm64

#     runs-on: ${{ matrix.os }}
#     env:
#       XVFB_PFX: ${{ (matrix.os == 'ubuntu-latest' && 'xvfb-run --auto-servernum -- ') || '' }}

#     steps:
#       - name: Check out Git repository
#         uses: actions/checkout@v4
#         with:
#           submodules: 'recursive'

#       - name: Install Node.js, NPM and Yarn
#         uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: 'yarn'

#       - name: Install build essentials (macOS)
#         if: matrix.os == 'macos-latest'
#         run: brew install rpm

#       - name: Install deps
#         run: ${{ env.XVFB_PFX }}yarn

#       - name: Rebuild native dependencies for current architecture
#         run: |
#           ${{ env.XVFB_PFX }}npm rebuild --target_arch=${{ matrix.arch }}
#           ${{ env.XVFB_PFX }}./node_modules/.bin/electron-rebuild --arch=${{ matrix.arch }}

#       - name: Build/release Electron app
#         shell: bash
#         env:
#           GH_TOKEN: ${{ secrets.github_token }}
#           ARCH: ${{ matrix.arch }}
#         run: yarn build --${{ matrix.arch }} -p always

#       - uses: actions/upload-artifact@v4
#         if: matrix.os == 'windows-latest'
#         with:
#           name: VutronMusic-win-installer-${{ matrix.arch }}
#           path: release/*/VutronMusic*_${{ matrix.arch }}.exe
#           if-no-files-found: ignore

#       - uses: actions/upload-artifact@v4
#         if: matrix.os == 'windows-latest'
#         with:
#           name: VutronMusic-win-portable-${{ matrix.arch }}
#           path: release/*/VutronMusic*_${{ matrix.arch }} Portable.exe
#           if-no-files-found: ignore

#       - uses: actions/upload-artifact@v4
#         if: matrix.os == 'macos-latest'
#         with:
#           name: VutronMusic-mac-${{ matrix.arch }}
#           path: release/*/VutronMusic*_${{ matrix.arch }}.dmg
#           if-no-files-found: ignore

#       - uses: actions/upload-artifact@v4
#         if: matrix.os == 'ubuntu-latest'
#         with:
#           name: VutronMusic-linux-snap-${{ matrix.arch }}
#           path: release/*/VutronMusic*.snap
#           if-no-files-found: ignore

#       - uses: actions/upload-artifact@v4
#         if: matrix.os == 'ubuntu-latest'
#         with:
#           name: VutronMusic-linux-AppImage-${{ matrix.arch }}
#           path: release/*/VutronMusic*.AppImage
#           if-no-files-found: ignore

#       - uses: actions/upload-artifact@v4
#         if: matrix.os == 'ubuntu-latest'
#         with:
#           name: VutronMusic-linux-deb-${{ matrix.arch }}
#           path: release/*/VutronMusic*.deb
#           if-no-files-found: ignore

#       - uses: actions/upload-artifact@v4
#         if: matrix.os == 'ubuntu-latest'
#         with:
#           name: VutronMusic-linux-rpm-${{ matrix.arch }}
#           path: release/*/VutronMusic*.rpm
#           if-no-files-found: ignore

name: Release

on:
  push:
    tags:
      - v*
  workflow_dispatch:

permissions: write-all

jobs:
  Release:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        arch: [x64]
        include:
          - os: macos-latest
            arch: arm64

    runs-on: ${{ matrix.os }}
    env:
      XVFB_PFX: ${{ (matrix.os == 'ubuntu-latest' && 'xvfb-run --auto-servernum -- ') || '' }}

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Install Node.js, NPM and Yarn
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'yarn'

      - name: Install build essentials (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install rpm

      - name: Install deps
        run: ${{ env.XVFB_PFX }}yarn

      - name: Rebuild native dependencies for current architecture
        run: |
          ${{ env.XVFB_PFX }}npm rebuild --target_arch=${{ matrix.arch }}
          ${{ env.XVFB_PFX }}./node_modules/.bin/electron-rebuild --arch=${{ matrix.arch }}

      - name: Build Electron app (without auto-publish)
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.github_token }}
          ARCH: ${{ matrix.arch }}
        run: yarn build --${{ matrix.arch }} -p never

      - uses: actions/upload-artifact@v4
        if: matrix.os == 'windows-latest'
        with:
          name: VutronMusic-win-installer-${{ matrix.arch }}
          path: release/*/VutronMusic*_${{ matrix.arch }}.exe
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v4
        if: matrix.os == 'windows-latest'
        with:
          name: VutronMusic-win-portable-${{ matrix.arch }}
          path: release/*/VutronMusic*_${{ matrix.arch }} Portable.exe
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v4
        if: matrix.os == 'macos-latest'
        with:
          name: VutronMusic-mac-${{ matrix.arch }}
          path: release/*/VutronMusic*_${{ matrix.arch }}.dmg
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v4
        if: matrix.os == 'ubuntu-latest'
        with:
          name: VutronMusic-linux-snap-${{ matrix.arch }}
          path: release/*/VutronMusic*.snap
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v4
        if: matrix.os == 'ubuntu-latest'
        with:
          name: VutronMusic-linux-AppImage-${{ matrix.arch }}
          path: release/*/VutronMusic*.AppImage
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v4
        if: matrix.os == 'ubuntu-latest'
        with:
          name: VutronMusic-linux-deb-${{ matrix.arch }}
          path: release/*/VutronMusic*.deb
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v4
        if: matrix.os == 'ubuntu-latest'
        with:
          name: VutronMusic-linux-rpm-${{ matrix.arch }}
          path: release/*/VutronMusic*.rpm
          if-no-files-found: ignore

  # 添加新的发布作业，等所有构建完成后统一上传到 GitHub Release
  publish-release:
    needs: Release
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-artifacts

      - name: Display structure of downloaded files
        run: ls -R all-artifacts

      - name: Publish to GitHub Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.github_token }}
        with:
          files: all-artifacts/**/*
          draft: false
          prerelease: false
